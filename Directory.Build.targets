<Project>

  <Target
    Name="ValidateProjectReferences"
    BeforeTargets="Build"
    Condition="'$(DesignTimeBuild)' != 'true'">

    <Message
      Importance="High"
      Text="Building $(MSBuildProjectName) for $(TargetFramework)" />

  </Target>

</Project>